{
  "name": "evomtrs-site",
  "version": "1.0.0",
  "private": true,
  "description": "EVOMTRS static marketing site build scripts",
  "scripts": {
    "build": "python3 scripts/render_site.py public dist",
    "build:example": "env -i PATH=\"$PATH\" EVOMTRS_ALLOW_PREVIEW_PLACEHOLDERS=1 python3 scripts/render_site.py public dist --env-file .env.example",
    "verify:placeholder-containment": "env -i PATH=\"$PATH\" EVOMTRS_SITE_URL=https://example.com EVOMTRS_PUBLIC_BASE_PATH=/subpath EVOMTRS_BUSINESS_NAME='EVO \"MTRS\"' python3 scripts/render_site.py public /tmp/evomtrs-placeholder-containment-dist --env-file .env.example && python3 scripts/verify_placeholder_containment.py /tmp/evomtrs-placeholder-containment-dist --expect-canonical-root https://example.com/subpath/ --expect-escaped-business-name 'EVO &quot;MTRS&quot;'",
    "verify:static": "python3 scripts/verify_static.py dist --env-file .env.example",
    "verify:launch-values": "python3 scripts/verify_launch_values.py dist --env-file .env.example",
    "verify:launch-local": "npm run build:example && npm run verify:static && npm run verify:launch-values",
    "verify:deploy-posture": "python3 scripts/verify_deploy_posture.py",
    "verify": "npm run build:example && npm run verify:static && npm run verify:deploy-posture && npm run verify:placeholder-containment",
    "clean": "rm -rf dist",
    "serve": "npm run build && npx http-server dist -p 8080",
    "deploy": "node -e \"console.error('GitHub Pages is the canonical production target; deploy approval is required before running the manual workflow.'); process.exit(1)\"",
    "legacy:cf:dev": "npm run build && npx wrangler pages dev dist --port 8788",
    "legacy:cf:deploy": "npm run build && wrangler pages deploy dist",
    "legacy:cf:preview": "wrangler dev"
  },
  "devDependencies": {
    "http-server": "latest",
    "wrangler": "^4.103.0"
  },
  "overrides": {
    "esbuild": "0.28.1"
  }
}
